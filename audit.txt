Confirmed: I can access `Clownworldenjoyer76/bet_tracker`.

## Full Audit Outline

### 1. Repo Map

Goal: identify the actual pipeline, not guess from filenames.

Deliverables:

* folder map
* Python script inventory
* workflow inventory
* input/output directory map
* stage order by sport

Focus:

* `docs/win/baseball`
* GitHub Actions workflows
* shared scripts under `/scripts`

---

### 2. MLB Pipeline Trace

Goal: trace one sport end-to-end before touching everything else.

Path:

```text
00_intake / 00_parsing
→ 01_merge
→ 02_juice
→ 03_edges
→ 04_select
→ 05_final_scores
→ reports / frontend output
```

Deliverables:

* each script’s inputs
* each script’s outputs
* required CSV headers
* join keys
* files that are duplicated, stale, unused, or conflicting

---

### 3. CSV Header + Schema Audit

Goal: find files that silently break downstream logic.

Checks:

* missing required columns
* duplicate columns
* renamed columns not handled downstream
* numeric fields stored as strings
* empty probability/odds columns
* inconsistent date/game ID formats
* home/away field reversals

Deliverable:

* exact file/script pairs where schema mismatch occurs

---

### 4. Merge Logic Audit

Goal: find bad joins and duplicate/dropped games.

Checks:

* `game_id` vs `gamePk`
* team-name normalization
* date matching
* home/away matching
* duplicate rows per game
* many-to-many merge risk
* missing odds or prediction rows after merge

Highest priority:

* `merge_intake.py`
* `build_juice_files.py`
* name normalization scripts

---

### 5. Odds + Vig/No-Vig Audit

Goal: confirm probability math is correct.

Checks:

* American → decimal conversion
* American → implied probability
* juiced probability
* normalized/no-vig probability
* favorite/underdog sign handling
* odds better/worse than thresholds
* stale/missing DraftKings values

Highest priority:

* `apply_moneyline_juice.py`
* `apply_run_line_juice.py`
* `apply_total_juice.py`

---

### 6. EV + Kelly Audit

Goal: confirm betting math is not selecting false value.

Checks:

* EV formula
* decimal odds usage
* probability source used for EV
* Kelly formula
* negative EV handling
* zero/blank Kelly handling
* edge direction: model vs market
* whether outputs are rounded before selection

Highest priority:

* `compute_edges.py`
* `compute_ev_kelly.py`

---

### 7. Bet Selection Audit

Goal: find why bad bets get selected or good bets get filtered out.

Checks:

* market filters
* min probability
* min EV
* odds thresholds
* duplicated games across markets
* one-side-per-game handling
* favorites/underdogs logic
* stale file selection
* config-driven rules vs hardcoded rules

Highest priority:

* `baseball_select_bets.py`
* any `markets.yaml` / config files

---

### 8. Data Leakage Audit

Goal: identify anything that uses final scores, closing results, or post-game data before selection.

Checks:

* final score files referenced upstream
* result/grading files imported before picks
* same-day file overwrite issues
* timestamp order problems
* future game data mixed with historical training data
* model training files using target/result fields

Deliverable:

* exact leakage paths, if present

---

### 9. Grading + Results Audit

Goal: make sure win/loss reporting is accurate.

Checks:

* moneyline grading
* run line grading
* total grading
* pushes
* canceled/postponed games
* doubleheaders
* home/away score direction
* duplicate graded rows
* ROI/win-rate calculations

Highest priority:

* `05_final_scores`
* `docs/win/final_scores`

---

### 10. Cross-Sport Reuse Audit

Goal: separate reusable design from copied broken logic.

Checks:

* baseball vs basketball vs hockey vs soccer script differences
* duplicated scripts with conflicting behavior
* old folders still used by workflows
* stale copies under `00_intake` and `00_parsing`
* inconsistent EV/Kelly implementation across sports

---

### 11. Workflow Audit

Goal: confirm GitHub Actions runs the intended files in the intended order.

Checks:

* manual trigger only
* secret usage
* script paths
* working directory
* output folders
* commit behavior
* workflow order
* failure handling
* whether generated CSVs are committed correctly

---

### 12. Final Findings Report

Format:

| Severity | Area | Exact File | Issue | Betting Impact | Fix Priority |
| -------- | ---- | ---------- | ----- | -------------- | ------------ |

Severity levels:

* **Critical**: wrong picks, leakage, broken EV/Kelly, bad grading
* **High**: dropped games, duplicate games, stale files
* **Medium**: fragile paths, missing validation
* **Low**: cleanup, readability, unused scripts

The audit should start with **MLB only**, then expand after the MLB pipeline is mapped.
