It **doesn’t.**

### Current script behavior (exact)

All selection logic is based on:

* **DK odds**

  * `*_dk_decimal_*`
  * `*_dk_*_american`

* **Model probabilities**

  * `*_normalized_prob_*`

* **Derived metrics**

  * `EV`
  * `Kelly`

---

### What your juiced odds are doing right now

Your **juiced odds** are only used **indirectly**:

They are used to create:

* `normalized_prob_*`

Which then feed:

```text
EV = normalized_prob * dk_decimal - 1
```

So:

→ Your juice influences **probability shaping**
→ But NOT **direct odds comparison**

---

### What is missing (your intent)

You said:

> "i will also want my personal juiced odds used to compare vs the raw dk odds"

That requires an **explicit comparison layer**, which is NOT implemented yet.

---

### What you need (exactly)

You need to add:

#### New column (per side)

```text
edge_vs_personal = (model_prob * personal_juiced_decimal) - 1
```

OR more directly (cleaner):

```text
price_edge = (1 / personal_juiced_decimal) - (1 / dk_decimal)
```

Interpretation:

* > 0 → your book thinks this is better than DK
* < 0 → DK is sharper than your model

---

### Where it should go

**NOT in select script logic**

Add it in:

```text
03_edges OR ev_kelly stage
```

Then selection can optionally filter on it.

---

### Why your current system still works

Because EV is already:

```text
EV = model_prob vs DK price
```

That is the **correct betting signal**

Your juiced odds are:

* calibration tool
* sanity check
* optional secondary filter

---

### Bottom line

* **Right now:** your juice only affects probabilities (indirect)
* **Not used:** for direct price comparison
* **Missing piece:** edge_vs_personal metric

---

### If you want it done cleanly

Next step:

* add `edge_vs_personal` into ev_kelly output
* optionally add config filter like:

```yaml
personal_edge_min: 0.01
```

That gives you:
→ model edge
→ market edge
→ self-consistency check

All three aligned.
