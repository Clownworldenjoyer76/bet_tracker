name: Final Step 01

on:
  workflow_dispatch:

jobs:
  run-final-step-01:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas

      - name: Run final_01 script
        run: |
          python scripts/final_01.py

      - name: Run final_02 script
        run: |
          python scripts/final_02.py

      - name: Run final_03 script
        run: |
          python scripts/final_03.py

      - name: Run final_04 script
        run: |
          python scripts/final_04.py

      - name: Run final_05 script
        run: |
          python scripts/final_05.py

      - name: Run final_06 script
        run: |
          python scripts/final_06.py

      - name: Commit and push results
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          if git status --porcelain | grep .; then
            git add docs/win/final/step_1 docs/win/final/step_2 docs/win/final/step_3
            git commit -m "Generate final step outputs (steps 1–3, edges)"
            git push
          else
            echo "No changes to commit"
          fi
