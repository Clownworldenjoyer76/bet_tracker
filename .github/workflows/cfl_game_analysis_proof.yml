name: CFL Game Analysis Proof

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  game-analysis-proof:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run CFL game analysis proof parser
        run: python docs/win/football/cfl/scripts/00_parser/cfl_game_analysis_proof.py

      - name: Commit proof outputs
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add docs/win/football/cfl/data_dump/proof/game_analysis/

          if git diff --cached --quiet; then
            echo "No proof output changes to commit"
          else
            git commit -m "data submitted"
            git push
          fi
