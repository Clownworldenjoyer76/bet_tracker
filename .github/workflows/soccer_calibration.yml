name: Soccer Calibration Build

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-calibration:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas

      - name: Run soccer calibration script
        run: |
          python scripts/soccer.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add bets/soccer/calibration/soccer_calibration_master.csv || true
          git commit -m "Update soccer calibration master file" || echo "No changes to commit"
          git push
