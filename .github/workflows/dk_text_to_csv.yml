name: DK Text to CSV

on:
  workflow_dispatch:
    inputs:
      league:
        required: true
        type: string
      raw_text:
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate league
        shell: bash
        run: |
          case "${{ inputs.league }}" in
            nba|ncaab|nhl|soc) ;;
            *) echo "invalid league"; exit 1 ;;
          esac

      - name: Write raw text safely
        shell: bash
        run: |
          mkdir -p docs/win/manual
          printf '%s\n' "${{ inputs.raw_text }}" > raw.txt

      - name: Parse DK text and write CSV
        shell: bash
        run: |
          python3 <<'PY'
          import csv
          from datetime import datetime

          LEAGUE = "${{ inputs.league }}"
          OUT = f"docs/win/manual/dk_{LEAGUE}_{datetime.now().strftime('%Y_%m_%d')}.csv"

          with open("raw.txt") as f:
              lines = [l.strip().replace("opens in a new tab","") for l in f if l.strip()]

          rows = []
          i = 0
          while i < len(lines):
              if "@" not in lines[i]:
                  i += 1
                  continue

              away, home = [x.strip() for x in lines[i].split("@", 1)]
              date_str, time_str = [x.strip() for x in lines[i+1].split(",", 1)]
              i += 2

              for lbl in ("Moneyline","Odds","% Handle","% Bets"):
                  if lines[i] != lbl:
                      raise SystemExit(f"format error: expected {lbl}, got {lines[i]}")
                  i += 1

              t1, o1, h1, b1 = lines[i:i+4]
              t2, o2, h2, b2 = lines[i+4:i+8]
              i += 8

              rows.append([date_str, time_str, t1, away, o1, h1[:-1], b1[:-1], LEAGUE])
              rows.append([date_str, time_str, t2, home, o2, h2[:-1], b2[:-1], LEAGUE])

          with open(OUT, "w", newline="") as f:
              w = csv.writer(f)
              w.writerow(["date","time","team","opponent","odds","handle_pct","bets_pct","league"])
              w.writerows(rows)
          PY

      - name: Commit CSV
        shell: bash
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add docs/win/manual/*.csv
          git commit -m "Add DK CSV" || true
          git push
