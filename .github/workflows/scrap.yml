name: Scraper

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install playwright pandas
          playwright install chromium

      - name: Run scraper
        run: python scripts/scrap/drat_scraper.py

      - name: Run transform
        run: python scripts/scrap/transform_predictions.py --input all_predictions.csv

      - name: Commit and push outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/ all_predictions.csv predictions_raw.json
          git diff --cached --quiet || git commit -m "scraper run $(date +'%Y-%m-%d %H:%M')"
          git push
