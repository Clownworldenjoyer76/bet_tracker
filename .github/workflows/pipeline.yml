name: 00 Run Pipeline

on:
  workflow_dispatch:
    inputs:
      slate_date:
        description: "Optional slate date (YYYY_MM_DD). Leave blank for today."
        required: false
        type: string

permissions:
  contents: write

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:

      # -----------------------------
      # Checkout repository
      # -----------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # -----------------------------
      # Setup Python
      # -----------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # -----------------------------
      # Install dependencies
      # -----------------------------
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy scipy

      # -----------------------------
      # Run pipeline
      # -----------------------------
      - name: Run pipeline
        run: |
          if [ -z "${{ github.event.inputs.slate_date }}" ]; then
            python run_pipeline.py
          else
            python run_pipeline.py "${{ github.event.inputs.slate_date }}"
          fi

      # -----------------------------
      # Show pipeline log (for debugging)
      # -----------------------------
      - name: Show pipeline log
        run: |
          echo "------ PIPELINE LOG ------"
          cat docs/win/errors/pipeline_log.txt || true

      # -----------------------------
      # Commit and push results
      # -----------------------------
      - name: Commit and push results
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # allow push
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git

          # stage everything changed
          git add docs/win || true
          git add mappings || true
          git add docs/win/errors/pipeline_log.txt || true

          # commit only if changes exist
          git diff --staged --quiet || git commit -m "Automated pipeline run"

          git push
