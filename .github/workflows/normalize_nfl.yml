name: Normalize NFL Historic Data

on:
  workflow_dispatch:

jobs:
  normalize-nfl:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas

      - name: Normalize NFL data
        run: |
          python scripts/normalize_nfl.py

      - name: Commit normalized NFL CSV
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add bets/historic/nfl_normalized.csv
          git commit -m "Normalize NFL historic odds data" || echo "No changes to commit"
          git push
